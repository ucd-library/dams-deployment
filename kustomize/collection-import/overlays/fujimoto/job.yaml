apiVersion: batch/v1
kind: Job
metadata:
  name: import-fujimoto
spec:
  template:
    spec:
      containers:
      - name: importer
        env:
          - name: COLLECTION_NAME
            value: fujimoto
        volumeMounts:
        - name: import-volume
          mountPath: /data
      volumes:
      - name: import-volume
        persistentVolumeClaim:
          claimName: import-fujimoto-volume-claim