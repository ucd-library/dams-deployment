apiVersion: batch/v1
kind: Job
metadata:
  name: import-schwier-album
spec:
  template:
    spec:
      containers:
      - name: importer
        env:
          - name: COLLECTION_NAME
            value: schwier-album
        volumeMounts:
        - name: import-volume
          mountPath: /data
      volumes:
      - name: import-volume
        persistentVolumeClaim:
          claimName: import-schwier-album-volume-claim