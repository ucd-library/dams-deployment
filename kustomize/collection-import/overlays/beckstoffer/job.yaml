apiVersion: batch/v1
kind: Job
metadata:
  name: import-beckstoffer
spec:
  template:
    spec:
      containers:
      - name: importer
        env:
          - name: COLLECTION_NAME
            value: beckstoffer
        volumeMounts:
        - name: import-volume
          mountPath: /data
      volumes:
      - name: import-volume
        persistentVolumeClaim:
          claimName: import-beckstoffer-volume-claim