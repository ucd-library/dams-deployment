apiVersion: v1
kind: PersistentVolume
metadata:
  name: gcs-fuse-pv
spec:
  storageClassName: microk8s-hostpath
  accessModes:
    - ReadWriteMany
  capacity:
    storage: 25Gi
  hostPath:
    path: /tmp/gcs-fuse-volume
